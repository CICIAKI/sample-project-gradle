pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'gradlew clean build'
            }
        }
  
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
